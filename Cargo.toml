[package]
name = "traiter"
description = "Traits collection."
license = "MIT"
version = "1.0.0"
edition = "2015"

[lib]
name = "traiter"

[features]
collections = []
numbers = []
std = []

[package.metadata.docs.rs]
all-features = true
